name: diesel_emissions
version: '1.0.0'
config-version: 2

profile: diesel_emissions

model-paths:    ["models"]
seed-paths:     ["seeds"]
test-paths:     ["tests"]
snapshot-paths: ["snapshots"]
analysis-paths: ["analyses"]
macro-paths:    ["macros"]

target-path:  "target"
clean-targets: ["target", "dbt_packages"]

models:
  diesel_emissions:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
